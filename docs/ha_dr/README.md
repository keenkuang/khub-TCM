# 双机热备 & 远程灾备 — 评审与修订存档

本目录按"每一步都落盘"的要求，保存方案评审全过程的可追溯记录。

## 文件清单

| 文件 | 内容 |
|------|------|
| `docs/disaster_recovery.md` | 权威定稿设计文档（vFinal） |
| `review_round1.md` | 第 1 轮三角色评审记录 + 评审修订记录 + 修订稿 |
| `review_round2.md` | 新一轮（替代原始会话缺失轮次）三角色评审记录 + 修订记录 + 修订稿 |
| `review_round3.md` | 第 3 轮（纯设计）三角色复审记录 + 修订记录 + 修订稿 |

## ⚠️ 关于原始会话的第 2、3 轮评审

经核对原始会话记录（`6c5a062a-...jsonl`），情况如下：

- **第 1 轮**（发起 `U[2863]`）：三视角（PM / 架构师 / 软件设计师）评审**完整留下正文**，修订结论已沉淀于 `review_round1.md` 与 `docs/disaster_recovery.md`。
- **第 2 轮**（发起 `U[2919]`「再来一轮三角色评审」）与 **第 3 轮**（发起 `U[2983]`「二次评审完了」、`U[2988]`「再评审一轮」）：在 `U[2919]` 与 `U[3018]`「第三轮修订好了，可以定稿实施」之间，助手回复**仅有一条 `429 too many requests`（限流）**，没有任何评审 / 修订正文留存。随后助手直接 `Plan approved ... start implementing P0`。

**结论：第 2、3 轮评审内容在原始会话中已真实丢失（限流导致未执行/未留存）。** 用户"怕弄掉了一些东西"的担忧成立。

因此，**当前 `review_round2.md` 是重新发起的一轮三角色评审**（而非原始会话里那两轮），用于替代缺失轮次、补全把关。后续任何评审均须如实落盘，避免再次丢失。
