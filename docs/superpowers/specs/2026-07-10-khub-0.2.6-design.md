# v0.2.6 设计说明 — Diff 对比 + 老问诊导入器 + 安全加固

## 功能
- 文档版本 Diff 对比（LCS 行级算法，零依赖）
- 老问诊系统导入器（Excel/HTML → 字段映射 → 患者/病历/问诊）
- EPUB 改用 defusedxml（XXE 防护）
- bandit 清零 + mypy 配置
- CI 工作流

## 文件
- `khub/diff.py` / `khub/importer.py` / `khub/.bandit`
