# v0.2.10 设计说明 — 课程运营管理系统

## 功能
- 课程 CRUD（名称/教师/时间/容量/价格/状态）
- 课时管理（按课程组织，日期/时间/地点/讲义）
- 学员报名（容量检查，超限报错）
- 成绩录入

## 表
- `courses` / `lessons` / `enrollments` / `grades`

## 接口
- REST: `/api/courses` 系列 9 端点
- CLI: `course-create/list/info` / `lesson-add/list` / `enroll` / `grade`
