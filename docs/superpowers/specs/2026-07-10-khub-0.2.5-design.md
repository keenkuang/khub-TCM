# v0.2.5 设计说明 — 数据看板 + 安全加固 + Docker 部署

## 功能
- `/stats` 扩展：近 7 天入库趋势、版本/向量数、来源分布
- 前端 SVG 图表（条形/折线图）
- `GET /sync-status` — 数据源同步状态面板
- 限流器持久化（SQLite 令牌桶）
- Docker 部署：Dockerfile + compose + nginx + CSP

## 表
- `ratelimit` 表（令牌桶持久化）
