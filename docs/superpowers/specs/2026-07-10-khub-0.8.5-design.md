# khub 0.8.5 实现计划 — 国际化

## 文件清单

| 文件 | 操作 | 职责 |
|------|------|------|
| `khub/i18n.py` | 创建 | 翻译引擎 + 语言检测 |
| `khub/api.py` | 修改 | `Accept-Language` 头支持 |
| `khub/web/index.html` | 修改 | 语言切换 UI |

## 实施

### 翻译引擎 — `khub/i18n.py`

```python
"""国际化——翻译引擎 + 语言检测。"""
from __future__ import annotations
import json, os

TRANSLATIONS: dict[str, dict[str, str]] = {
    "zh": {
        "app.name": "kHUB 个人知识中枢",
        "nav.search": "搜索", "nav.stats": "统计", "nav.ops": "运营",
        "nav.course": "课程", "nav.admin": "管理", "nav.logout": "注销",
        "login.title": "kHUB 登录", "login.username": "用户名",
        "login.password": "密码", "login.btn": "登录",
        "common.loading": "加载中…", "common.no_data": "暂无数据",
        "common.error": "加载失败", "common.save": "保存", "common.cancel": "取消",
    },
    "en": {
        "app.name": "kHUB Knowledge Hub",
        "nav.search": "Search", "nav.stats": "Stats", "nav.ops": "Operations",
        "nav.course": "Courses", "nav.admin": "Admin", "nav.logout": "Logout",
        "login.title": "kHUB Login", "login.username": "Username",
        "login.password": "Password", "login.btn": "Sign In",
        "common.loading": "Loading…", "common.no_data": "No Data",
        "common.error": "Load Failed", "common.save": "Save", "common.cancel": "Cancel",
    },
}

def t(key: str, lang: str = "zh") -> str:
    return TRANSLATIONS.get(lang, {}).get(key, TRANSLATIONS.get("zh", {}).get(key, key))

def detect_lang(accept_language: str = "") -> str:
    if accept_language.startswith("en"):
        return "en"
    return "zh"

def get_translations(lang: str) -> dict:
    return TRANSLATIONS.get(lang, TRANSLATIONS["zh"])
```

### API — `khub/api.py`

`GET /api/i18n` 端点：
```python
if method == "GET" and path == "/api/i18n":
    from .i18n import detect_lang, get_translations
    lang = detect_lang(auth_header or "zh")
    return 200, {"lang": lang, "translations": get_translations(lang)}
```

### Web UI — `khub/web/index.html`

语言切换下拉 + 动态加载翻译：
```html
<select id="langSelect" onchange="switchLang(this.value)" style="font-size:12px;margin-left:8px">
  <option value="zh">中文</option>
  <option value="en">English</option>
</select>
```

`script.js`：
```javascript
var _translations = {};

async function loadTranslations() {
  try {
    var r = await fetch('/api/i18n').then(function(x){return x.json();});
    _translations = r.translations || {};
    document.getElementById('langSelect').value = r.lang || 'zh';
    applyTranslations();
  } catch(e) {}
}

function _(key) { return _translations[key] || key; }

function applyTranslations() {
  document.title = _('app.name');
  document.querySelectorAll('[data-i18n]').forEach(function(el){
    el.textContent = _(el.getAttribute('data-i18n'));
  });
}

async function switchLang(lang) {
  try {
    var r = await fetch('/api/i18n').then(function(x){return x.json();});
    _translations = r.translations || {};
    applyTranslations();
  } catch(e) {}
}
```

### 测试 — `tests/test_i18n.py`

```python
from khub.i18n import t, detect_lang, get_translations

def test_zh():
    assert t("app.name", "zh") == "kHUB 个人知识中枢"

def test_en():
    assert t("app.name", "en") == "kHUB Knowledge Hub"

def test_fallback():
    assert t("unknown.key", "en") == "unknown.key"

def test_detect_zh():
    assert detect_lang("zh-CN,zh;q=0.9") == "zh"

def test_detect_en():
    assert detect_lang("en-US,en;q=0.9") == "en"

def test_detect_default():
    assert detect_lang("") == "zh"
```
