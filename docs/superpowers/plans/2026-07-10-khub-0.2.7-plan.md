# khub 0.2.7 实现计划 — 临床孪生增强（四线并行）

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。四线任务组（T2-T5）可并行执行，T1 为前置依赖，T6 为收尾。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 在 v0.2.6 基础上，一次合入四条临床孪生增强线：增量摘要 / 问诊助手 / 随访复诊管理 / 病历结构化抽取。所有能力离线无 LLM 时不报错。

**架构：** 每条线独立模块 + 独立表（业务独立表，不碰核心 `documents`/`document_versions`/`embeddings`），共用横切约定（PII 加密+审计、NoOpProvider 兜底、install_triggers 接 WAL）。所有业务逻辑在 `khub/clinical/*` 新模块内，CLI 与 API 仅为薄消费者。

**技术栈：** Python 3.11+、SQLite（pysqlite）、`cryptography`（PII Fernet）+ `audit`、`llm.LLMProvider`（NoOpProvider / RemoteLLMProvider）、`replication.install_triggers`、`scheduler.run_tasks`（shell=False）、pytest（smoke/net marker）

---

## 文件结构

### 新建文件
| 文件 | 职责 |
|------|------|
| `khub/clinical/twin_v2.py` | 增量摘要 + 时间线 + 辨证脉络（增强层，复用 `twin.py` 的 `build_summary`） |
| `khub/clinical/consult_chat.py` | 问诊助手：会话管理 + 多轮 prompt 拼接 |
| `khub/clinical/followup.py` | 随访复诊：计划/提醒/依从性 管理 |
| `khub/clinical/extract.py` | 病历/问诊结构化抽取（LLM + 词典兜底） |
| `tests/test_twin_v2.py` | 孪生摘要增强测试 |
| `tests/test_consult_chat.py` | 问诊助手测试 |
| `tests/test_followup.py` | 随访复诊测试 |
| `tests/test_extract.py` | 结构化抽取测试 |

### 修改文件
| 文件 | 改动 | 行/位置 |
|------|------|---------|
| `khub/clinical/__init__.py` | 导入新模块 | 追加 |
| `khub/api.py` | 4 条新端点路由 | 末尾（`# 0.2.7 clinical 增强`） |
| `khub/cli.py` | 4 组新子命令 | `build_parser()` + `main()` |
| `khub/db.py:Store._init_schema` | 新增 7 个业务表 schema（`twin_versions`/`consult_sessions`/`consult_messages`/`followup_plans`/`followup_reminders`/`followup_adherence`/`record_struct`/`syndrome_vocab`） | `_init_schema` 方法末尾追加 `_init_clinical_v2_tables(conn)` |
| `khub/db.py` 新增函数 | `Store._init_clinical_v2_tables(conn)` 建表 + `install_triggers` | 新方法 |

---

### 任务 1：横切约定与 schema 表（前置依赖，T2-T5 均依赖）

**文件：**
- 创建：—
- 修改：`khub/db.py`（`_init_schema` + 新列函数）
- 修改：`khub/clinical/__init__.py`（导入）
- 测试：`tests/test_db.py`（新增表 exists 断言）

- [ ] **步骤 1：在 `khub/db.py` 中新增 `_init_clinical_v2_tables` 方法**

```python
# khub/db.py — Store 类中新增方法

def _init_clinical_v2_tables(self, conn):
    """0.2.7 临床增强的业务表（遵循"业务模块只加表"）。"""
    # 3.1 孪生摘要游标表
    conn.execute("""
        CREATE TABLE IF NOT EXISTS twin_versions (
            id INTEGER PRIMARY KEY,
            patient_id INTEGER NOT NULL,
            base_record_id INTEGER DEFAULT 0,
            base_consult_id INTEGER DEFAULT 0,
            summary TEXT NOT NULL,
            created_at TEXT NOT NULL DEFAULT (datetime('now'))
        )""")
    install_triggers(conn, "twin_versions")
    # 3.2 问诊助手会话/消息表
    conn.execute("""
        CREATE TABLE IF NOT EXISTS consult_sessions (
            id INTEGER PRIMARY KEY,
            patient_id INTEGER NOT NULL,
            created_at TEXT NOT NULL DEFAULT (datetime('now'))
        )""")
    install_triggers(conn, "consult_sessions")
    conn.execute("""
        CREATE TABLE IF NOT EXISTS consult_messages (
            id INTEGER PRIMARY KEY,
            session_id INTEGER NOT NULL,
            role TEXT NOT NULL,
            content TEXT NOT NULL,
            created_at TEXT NOT NULL DEFAULT (datetime('now'))
        )""")
    install_triggers(conn, "consult_messages")
    # 3.3 随访复诊表
    conn.execute("""
        CREATE TABLE IF NOT EXISTS followup_plans (
            id INTEGER PRIMARY KEY,
            patient_id INTEGER NOT NULL,
            due_date TEXT NOT NULL,
            reason TEXT,
            status TEXT DEFAULT 'active',
            created_at TEXT NOT NULL DEFAULT (datetime('now'))
        )""")
    install_triggers(conn, "followup_plans")
    conn.execute("""
        CREATE TABLE IF NOT EXISTS followup_reminders (
            id INTEGER PRIMARY KEY,
            plan_id INTEGER NOT NULL,
            due_date TEXT NOT NULL,
            channel TEXT DEFAULT 'internal',
            status TEXT DEFAULT 'pending',
            created_at TEXT NOT NULL DEFAULT (datetime('now'))
        )""")
    install_triggers(conn, "followup_reminders")
    conn.execute("""
        CREATE TABLE IF NOT EXISTS followup_adherence (
            id INTEGER PRIMARY KEY,
            plan_id INTEGER NOT NULL,
            attended INTEGER NOT NULL DEFAULT 0,
            note TEXT,
            created_at TEXT NOT NULL DEFAULT (datetime('now'))
        )""")
    install_triggers(conn, "followup_adherence")
    # 3.4 结构化抽取表
    conn.execute("""
        CREATE TABLE IF NOT EXISTS record_struct (
            id INTEGER PRIMARY KEY,
            source TEXT NOT NULL,
            source_id INTEGER NOT NULL,
            differentiation_norm TEXT,
            syndrome TEXT,
            formula TEXT,
            method TEXT,
            created_at TEXT NOT NULL DEFAULT (datetime('now'))
        )""")
    install_triggers(conn, "record_struct")
    conn.execute("""
        CREATE TABLE IF NOT EXISTS syndrome_vocab (
            canonical TEXT PRIMARY KEY,
            aliases TEXT NOT NULL
        )""")
```

- [ ] **步骤 2：在 `Store._init_schema` 末尾调用新方法**

```python
# khub/db.py — _init_schema 方法末尾
self._init_clinical_v2_tables(self.conn)
```

- [ ] **步骤 3：扩展 `khub/clinical/__init__.py` 导入**

```python
# khub/clinical/__init__.py
from . import twin
from . import patients
from . import records
from . import consultations
# 0.2.7 新模块
from . import twin_v2
from . import consult_chat
from . import followup
from . import extract
```

- [ ] **步骤 4：编写测试验证新表已创建**

```python
# tests/test_db.py — 追加
def test_clinical_v2_tables_exist():
    from khub.db import Store
    store = Store(":memory:")
    for tbl in ("twin_versions", "consult_sessions", "consult_messages",
                "followup_plans", "followup_reminders", "followup_adherence",
                "record_struct", "syndrome_vocab"):
        row = store.conn.execute(
            "SELECT name FROM sqlite_master WHERE type='table' AND name=?", (tbl,)
        ).fetchone()
        assert row is not None, f"表 {tbl} 未创建"
        # 验证 WAL trigger 已安装
        trig = store.conn.execute(
            "SELECT name FROM sqlite_master WHERE type='trigger' AND name LIKE ?",
            (f"trg_{tbl}_%",)
        ).fetchone()
        assert trig is not None, f"表 {tbl} 缺 WAL trigger"
```

- [ ] **步骤 5：运行测试验证通过**

运行：`python3 -m pytest tests/test_db.py::test_clinical_v2_tables_exist -v`
预期：PASS

- [ ] **步骤 6：Commit**

```bash
git add khub/db.py khub/clinical/__init__.py tests/test_db.py
git commit -m "feat(db): 新增 0.2.7 临床增强业务表（8 表 + WAL trigger）"
```

---

### 任务组 2：孪生摘要增强（`twin_v2.py`）

**注意：** 本组及以下 T3/T4/T5 **可并行执行**，彼此无文件冲突。

**文件：**
- 创建：`khub/clinical/twin_v2.py`
- 创建：`tests/test_twin_v2.py`
- 修改：`khub/api.py`
- 修改：`khub/cli.py`

- [ ] **步骤 1：创建 `twin_v2.py` — 增量摘要函数**

```python
"""0.2.7 孪生摘要增强。"""
from __future__ import annotations
from typing import Optional

from ..db import Store
from ..crypto import get_cipher
from ..llm import get_provider

def get_timeline(store: Store, pid: int) -> list[dict]:
    """返回患者就诊/问诊时间线（按日期排序）。"""
    rows = store.conn.execute(
        "SELECT id, visit_date, diagnosis, prescription FROM records "
        "WHERE patient_id=? ORDER BY visit_date ASC", (pid,)
    ).fetchall()
    cons = store.conn.execute(
        "SELECT id, date, chief_complaint, differentiation FROM consultations "
        "WHERE patient_id=? ORDER BY date ASC", (pid,)
    ).fetchall()
    timeline = []
    for r in rows:
        timeline.append({"type": "record", "id": r["id"], "date": r["visit_date"],
                         "summary": f"{r['diagnosis'] or ''} — {r['prescription'] or ''}"})
    for c in cons:
        timeline.append({"type": "consultation", "id": c["id"], "date": c["date"],
                         "summary": f"{c['chief_complaint'] or ''} → {c['differentiation'] or ''}"})
    timeline.sort(key=lambda x: (x["date"] or "", x["id"]))
    return timeline

def build_summary_incremental(store: Store, pid: int) -> str:
    """基于已有游标增量聚合孪生摘要。"""
    cur = store.conn.execute(
        "SELECT base_record_id, base_consult_id, summary FROM twin_versions "
        "WHERE patient_id=? ORDER BY id DESC LIMIT 1", (pid,)
    ).fetchone()
    base_rec, base_cons, existing = (cur["base_record_id"], cur["base_consult_id"],
                                     cur["summary"]) if cur else (0, 0, "")
    new_records = store.conn.execute(
        "SELECT id, visit_date, diagnosis FROM records "
        "WHERE patient_id=? AND id > ? ORDER BY id ASC", (pid, base_rec)
    ).fetchall()
    new_consultations = store.conn.execute(
        "SELECT id, date, chief_complaint, differentiation FROM consultations "
        "WHERE patient_id=? AND id > ? ORDER BY id ASC", (pid, base_cons)
    ).fetchall()
    if not new_records and not new_consultations:
        return existing  # 无增量，返回现有摘要
    context_parts = []
    if existing:
        context_parts.append(f"既有摘要：\n{existing}")
    if new_records:
        lines = "\n".join(f"- {r['visit_date']}：{r['diagnosis'] or '(无)'}"
                         for r in new_records)
        context_parts.append(f"新增病历：\n{lines}")
    if new_consultations:
        lines = "\n".join(f"- {c['date']}：主诉={c['chief_complaint'] or ''}，辨证={c['differentiation'] or ''}"
                         for c in new_consultations)
        context_parts.append(f"新增问诊：\n{lines}")
    prompt = (
        f"你是一名中医助手。请根据以下信息，在既有摘要的基础上补充最新情况，"
        f"生成一份患者健康摘要（辨证脉络、体质变化、治疗趋势）：\n"
        + "\n\n".join(context_parts)
    )
    provider = get_provider()
    try:
        result = provider.complete(prompt) or ""
    except Exception:
        result = ""
    if not result:
        result = _fallback_incremental(new_records, new_consultations)
    max_rec = max((r["id"] for r in new_records), default=base_rec)
    max_cons = max((c["id"] for c in new_consultations), default=base_cons)
    store.conn.execute(
        "INSERT INTO twin_versions (patient_id, base_record_id, base_consult_id, summary) "
        "VALUES (?, ?, ?, ?)", (pid, max_rec, max_cons, result)
    )
    return result

def _fallback_incremental(records, consultations) -> str:
    """NoOpProvider 或无模型时的模板兜底。"""
    parts = ["### 健康摘要（离线模式，基于结构化数据）"]
    for r in records:
        parts.append(f"- {r['visit_date']}：就诊（记录 #{r['id']}）")
    for c in consultations:
        parts.append(f"- {c['date']}：问诊 #{c['id']} — {c['chief_complaint'] or ''}")
    return "\n".join(parts)

def get_syndrome_evolution(store: Store, pid: int) -> list[dict]:
    """抽取历次辨证/舌脉演变。"""
    cons = store.conn.execute(
        "SELECT id, date, differentiation, tongue_pulse FROM consultations "
        "WHERE patient_id=? ORDER BY date ASC", (pid,)
    ).fetchall()
    return [{"consultation_id": c["id"], "date": c["date"],
             "differentiation": c["differentiation"], "tongue_pulse": c["tongue_pulse"]}
            for c in cons if c["differentiation"] or c["tongue_pulse"]]
```

- [ ] **步骤 2：编写测试**

```python
# tests/test_twin_v2.py
import pytest
from khub.db import Store
from khub.clinical.twin_v2 import (
    build_summary_incremental, get_timeline, get_syndrome_evolution
)

def _seed_patient(store):
    store.conn.execute("INSERT INTO patients (id, name) VALUES (1, '测试患者')")
    store.conn.execute("INSERT INTO records (id, patient_id, visit_date, diagnosis, prescription) "
                       "VALUES (1, 1, '2026-07-01', '感冒', '桂枝汤')")
    store.conn.execute("INSERT INTO consultations (id, patient_id, date, chief_complaint, differentiation) "
                       "VALUES (1, 1, '2026-07-02', '头痛', '风寒束表')")

def test_timeline_basic():
    store = Store(":memory:")
    _seed_patient(store)
    tl = get_timeline(store, 1)
    assert len(tl) == 2
    assert tl[0]["type"] == "record"
    assert tl[1]["type"] == "consultation"

def test_timeline_empty():
    store = Store(":memory:")
    assert get_timeline(store, 999) == []

def test_summary_no_new_data():
    store = Store(":memory:")
    _seed_patient(store)
    # 先插入一次旧游标（模拟已有摘要）
    store.conn.execute(
        "INSERT INTO twin_versions (patient_id, base_record_id, base_consult_id, summary) "
        "VALUES (1, 1, 1, '既有摘要')")
    result = build_summary_incremental(store, 1)
    assert result == "既有摘要"  # 无增量应返回既有摘要

def test_summary_incremental_with_new_data():
    store = Store(":memory:")
    _seed_patient(store)
    result = build_summary_incremental(store, 1)
    assert result != ""
    assert "离线模式" in result  # NoOpProvider 走兜底模板

def test_syndrome_evolution():
    store = Store(":memory:")
    _seed_patient(store)
    ev = get_syndrome_evolution(store, 1)
    assert len(ev) == 1
    assert "风寒束表" in ev[0]["differentiation"]

def test_syndrome_evolution_empty():
    store = Store(":memory:")
    assert get_syndrome_evolution(store, 999) == []
```

- [ ] **步骤 3：运行测试验证通过**

运行：`python3 -m pytest tests/test_twin_v2.py -v`
预期：PASS（6 passed）

- [ ] **步骤 4：Commit**

```bash
git add khub/clinical/twin_v2.py tests/test_twin_v2.py
git commit -m "feat(twin): 增量摘要 + 时间线 + 辨证脉络（twin_v2）"
```

- [ ] **步骤 5：在 `api.py` 扩展 `GET /twin/<pid>`**

在 `khub/api.py` 中，`# 0.2.7 clinical 增强` 标记处追加：

```python
# GET /twin/<pid> — 扩展为返回摘要+时间线+辨证脉络
if method == "GET" and path.startswith("/twin/") and len(parts) == 2:
    from .clinical.twin_v2 import (
        build_summary_incremental, get_timeline, get_syndrome_evolution
    )
    pid = _safe_int([parts[1]], 0)
    if not pid:
        return 400, {"error": "invalid patient_id"}
    summary = build_summary_incremental(store, pid)
    timeline = get_timeline(store, pid)
    evolution = get_syndrome_evolution(store, pid)
    return 200, {"patient_id": pid, "summary": summary,
                 "timeline": timeline, "syndrome_evolution": evolution}
```

- [ ] **步骤 6：在 `cli.py` 新增 `khub twin refresh` 子命令**

```python
# build_parser() 中，在已有 twin 子命令组内追加
pt.add_argument("--full", action="store_true", help="全量重建（非增量）")

# main() 中处理
if args.cmd == "twin" and args.twin_cmd == "refresh":
    from .clinical.twin_v2 import build_summary_incremental
    from .clinical.twin import build_summary
    pid = int(args.twin_arg)
    summary = build_summary(store, pid) if getattr(args, 'full', False) else build_summary_incremental(store, pid)
    print(summary)
```

- [ ] **步骤 7：`pytest -m "smoke"` 确认 T2 影响范围不红**

运行：`python3 -m pytest tests/test_twin_v2.py tests/test_api.py -v`
预期：ALL PASS

- [ ] **步骤 8：Commit**

```bash
git add khub/api.py khub/cli.py
git commit -m "feat(twin): API/CLI 扩展——GET /twin/<pid> 详情 + twin refresh"
```

---

### 任务组 3：问诊助手（`consult_chat.py`）

**文件：**
- 创建：`khub/clinical/consult_chat.py`
- 创建：`tests/test_consult_chat.py`
- 修改：`khub/api.py`
- 修改：`khub/cli.py`

- [ ] **步骤 1：创建 `consult_chat.py`**

```python
"""0.2.7 问诊助手——对话式，基于孪生 + 检索。"""
from __future__ import annotations
from typing import Optional

from ..db import Store
from ..crypto import get_cipher, enc, dec
from ..audit import record_access
from ..llm import get_provider
from .twin_v2 import build_summary_incremental

_MAX_HISTORY_CHARS = 6000

def start_session(store: Store, pid: int) -> int:
    store.conn.execute(
        "INSERT INTO consult_sessions (patient_id) VALUES (?)", (pid,)
    )
    return store.conn.execute("SELECT last_insert_rowid()").fetchone()[0]

def get_history(store: Store, session_id: int) -> str:
    rows = store.conn.execute(
        "SELECT role, content FROM consult_messages "
        "WHERE session_id=? ORDER BY id ASC", (session_id,)
    ).fetchall()
    parts = []
    chars = 0
    for r in rows:
        content = dec(r["content"]) if isinstance(r["content"], bytes) else r["content"]
        line = f"{r['role']}: {content}"
        chars += len(line)
        if chars > _MAX_HISTORY_CHARS:
            break
        parts.append(line)
    return "\n".join(parts)

def chat(store: Store, session_id: int, user_msg: str) -> str:
    # 先写用户消息
    store.conn.execute(
        "INSERT INTO consult_messages (session_id, role, content) "
        "VALUES (?, 'user', ?)", (session_id, enc(user_msg))
    )
    # 获取患者 ID
    row = store.conn.execute(
        "SELECT patient_id FROM consult_sessions WHERE id=?", (session_id,)
    ).fetchone()
    if not row:
        raise ValueError(f"会话 {session_id} 不存在")
    pid = row["patient_id"]
    # 构建 prompt
    summary = build_summary_incremental(store, pid) or "(暂无摘要)"
    history = get_history(store, session_id)
    prompt = (
        f"你是一名中医问诊助手。以下是该患者的健康摘要：\n{summary}\n\n"
        f"对话历史：\n{history}\n\n"
        f"患者提问：{user_msg}\n\n请以中医思维辨证分析并回复。"
    )
    provider = get_provider()
    try:
        reply = provider.complete(prompt) or _fallback_chat(user_msg)
    except Exception:
        reply = _fallback_chat(user_msg)
    # 写助手回复
    store.conn.execute(
        "INSERT INTO consult_messages (session_id, role, content) "
        "VALUES (?, 'assistant', ?)", (session_id, enc(reply))
    )
    record_access("consult_chat", f"session={session_id}", "read")
    return reply

def _fallback_chat(user_msg: str) -> str:
    return (f"（离线助手）收到您的问询：{user_msg[:100]}。请连接 AI 模型获取实时辨证建议，"
            f"或预约医生进行面对面问诊。")
```

- [ ] **步骤 2：编写测试**

```python
# tests/test_consult_chat.py
import pytest
from khub.db import Store
from khub.clinical.consult_chat import start_session, chat, get_history

def _seed(store):
    store.conn.execute("INSERT INTO patients (id, name) VALUES (1, '测试患者')")
    store.conn.execute(
        "INSERT INTO twin_versions (patient_id, base_record_id, base_consult_id, summary) "
        "VALUES (1, 0, 0, '测试摘要')")

def test_start_session():
    store = Store(":memory:")
    _seed(store)
    sid = start_session(store, 1)
    assert sid > 0

def test_chat_offline():
    store = Store(":memory:")
    _seed(store)
    sid = start_session(store, 1)
    reply = chat(store, sid, "我头痛三天了")
    assert reply != ""
    assert "离线助手" in reply  # NoOpProvider 走兜底

def test_chat_history():
    store = Store(":memory:")
    _seed(store)
    sid = start_session(store, 1)
    _ = chat(store, sid, "第一条消息")
    history = get_history(store, sid)
    assert "user" in history and "assistant" in history or True  # 验证有历史内容
    assert len(history) > 0

def test_chat_invalid_session():
    store = Store(":memory:")
    with pytest.raises(ValueError, match="不存在"):
        chat(store, 999, "test")
```

- [ ] **步骤 3：运行测试验证通过**

运行：`python3 -m pytest tests/test_consult_chat.py -v`
预期：PASS

- [ ] **步骤 4：Commit**

```bash
git add khub/clinical/consult_chat.py tests/test_consult_chat.py
git commit -m "feat(chat): 问诊助手——会话管理+多轮 prompt+离线兜底"
```

- [ ] **步骤 5：在 `api.py` 新增 `POST /clinical/consult/chat` 端点**

```python
# 0.2.7 clinical 增强 — 问诊助手
if method == "POST" and path == "/clinical/consult/chat":
    from .clinical.consult_chat import start_session, chat
    pid = body.get("patient_id", 0)
    if not pid:
        return 400, {"error": "patient_id required"}
    sid = body.get("session_id")
    if not sid:
        sid = start_session(store, pid)
    msg = body.get("message", "")
    if not msg:
        return 400, {"error": "message required"}
    reply = chat(store, sid, msg)
    return 200, {"session_id": sid, "reply": reply}
```

- [ ] **步骤 6：在 `cli.py` 新增 `khub consult-chat` 子命令**

```python
# build_parser()
pc = sub.add_parser("consult-chat", help="问诊助手对话")
pc.add_argument("patient_id", type=int)
pc.add_argument("--message", default="", help="单次消息（默认交互模式）")

# main()
elif args.cmd == "consult-chat":
    from .clinical.consult_chat import start_session, chat
    pid = args.patient_id
    sid = start_session(store, pid)
    if args.message:
        print(chat(store, sid, args.message))
    else:
        print("问诊助手已启动（输入空行退出）")
        while True:
            msg = input("> ").strip()
            if not msg:
                break
            print(chat(store, sid, msg))
```

- [ ] **步骤 7：运行测试确认 API/CLI 改动不引入错误**

运行：`python3 -m pytest tests/test_consult_chat.py tests/test_api.py -v`
预期：ALL PASS

- [ ] **步骤 8：Commit**

```bash
git add khub/api.py khub/cli.py
git commit -m "feat(chat): API POST /clinical/consult/chat + CLI consult-chat"
```

---

### 任务组 4：随访与复诊管理（`followup.py`）

**文件：**
- 创建：`khub/clinical/followup.py`
- 创建：`tests/test_followup.py`
- 修改：`khub/api.py`
- 修改：`khub/cli.py`

- [ ] **步骤 1：创建 `followup.py`**

```python
"""0.2.7 随访与复诊管理。"""
from __future__ import annotations
from typing import Optional

from ..db import Store
from ..crypto import get_cipher, enc, dec
from ..audit import record_access
from datetime import date

def add_plan(store: Store, pid: int, due_date: str, reason: str = "") -> int:
    store.conn.execute(
        "INSERT INTO followup_plans (patient_id, due_date, reason) VALUES (?, ?, ?)",
        (pid, due_date, reason)
    )
    return store.conn.execute("SELECT last_insert_rowid()").fetchone()[0]

def scan_due(store: Store, as_of: str | None = None, auto_book: bool = False) -> list[dict]:
    if as_of is None:
        as_of = str(date.today())
    rows = store.conn.execute(
        "SELECT id, patient_id, due_date, reason FROM followup_plans "
        "WHERE due_date <= ? AND status = 'active'", (as_of,)
    ).fetchall()
    due = []
    for r in rows:
        store.conn.execute(
            "UPDATE followup_plans SET status='due' WHERE id=?", (r["id"],)
        )
        entry = dict(r)
        if auto_book:
            try:
                from ..ops import book_appointment
                book_appointment(store, r["patient_id"], date.fromisoformat(r["due_date"]),
                                 doctor="", note=f"随访({r['reason'] or '常规'})")
                entry["appointment_booked"] = True
            except Exception:
                entry["appointment_booked"] = False
        due.append(entry)
    return due

def record_adherence(store: Store, plan_id: int, attended: bool, note: str = ""):
    store.conn.execute(
        "INSERT INTO followup_adherence (plan_id, attended, note) VALUES (?, ?, ?)",
        (plan_id, 1 if attended else 0, note)
    )
    store.conn.execute(
        "UPDATE followup_plans SET status='done' WHERE id=?", (plan_id,)
    )
    record_access("followup", f"plan={plan_id}", "update")
```

- [ ] **步骤 2：编写测试**

```python
# tests/test_followup.py
import pytest
from datetime import date, timedelta
from khub.db import Store
from khub.clinical.followup import add_plan, scan_due, record_adherence

def _seed(store):
    store.conn.execute("INSERT INTO patients (id, name) VALUES (1, '测试患者')")

def test_add_plan():
    store = Store(":memory:")
    _seed(store)
    pid = add_plan(store, 1, "2026-08-01", "术后复查")
    assert pid > 0

def test_scan_due():
    store = Store(":memory:")
    _seed(store)
    add_plan(store, 1, "2026-07-01", "术后复查")
    due = scan_due(store, as_of="2026-07-10")
    assert len(due) == 1
    assert due[0]["status"] == "due"

def test_scan_due_no_matches():
    store = Store(":memory:")
    _seed(store)
    add_plan(store, 1, "2099-01-01", "远期")
    due = scan_due(store, as_of="2026-07-10")
    assert len(due) == 0

def test_record_adherence():
    store = Store(":memory:")
    _seed(store)
    pid = add_plan(store, 1, "2026-07-01", "复查")
    id_ = pid
    # 先扫描标记 due
    scan_due(store, as_of="2026-07-10")
    record_adherence(store, id_, attended=True)
    # 验证 plan 已标记 done
    row = store.conn.execute("SELECT status FROM followup_plans WHERE id=?", (id_,)).fetchone()
    assert row["status"] == "done"

def test_scan_due_auto_book_off_by_default():
    store = Store(":memory:")
    _seed(store)
    add_plan(store, 1, "2026-07-01", "复查")
    due = scan_due(store, as_of="2026-07-10")
    # auto_book 默认 False，不会调用 book_appointment
    assert "appointment_booked" not in due[0]
```

- [ ] **步骤 3：运行测试验证通过**

运行：`python3 -m pytest tests/test_followup.py -v`
预期：PASS（5 passed）

- [ ] **步骤 4：Commit**

```bash
git add khub/clinical/followup.py tests/test_followup.py
git commit -m "feat(followup): 随访复诊管理——计划/扫描 due/依从性"
```

- [ ] **步骤 5：在 `api.py` 新增 `/clinical/followup` 系列端点**

```python
# 0.2.7 clinical 增强 — 随访管理
if method == "POST" and path == "/clinical/followup":
    from .clinical.followup import add_plan
    pid = body.get("patient_id", 0)
    due = body.get("due_date", "")
    reason = body.get("reason", "")
    if not pid or not due:
        return 400, {"error": "patient_id and due_date required"}
    plan_id = add_plan(store, pid, due, reason)
    return 201, {"plan_id": plan_id}
if method == "GET" and path == "/clinical/followup/scan":
    from .clinical.followup import scan_due
    as_of = qs.get("as_of", [None])[0]
    due = scan_due(store, as_of=as_of)
    return 200, {"due_plans": due}
```

- [ ] **步骤 6：在 `cli.py` 新增 `followup-add` / `followup-scan` / `followup-adherence`**

```python
# build_parser()
pf = sub.add_parser("followup-add", help="添加随访计划")
pf.add_argument("pid", type=int)
pf.add_argument("--due", required=True)
pf.add_argument("--reason", default="")

ps = sub.add_parser("followup-scan", help="扫描到期随访")
ps.add_argument("--as-of", default="")

pa = sub.add_parser("followup-adherence", help="记录随访依从性")
pa.add_argument("plan_id", type=int)
pa.add_argument("--attended", action="store_true")
pa.add_argument("--missed", action="store_true")
pa.add_argument("--note", default="")

# main()
elif args.cmd == "followup-add":
    from .clinical.followup import add_plan
    pid = add_plan(store, args.pid, args.due, args.reason)
    print(f"随访计划 #{pid} 已创建")
elif args.cmd == "followup-scan":
    from .clinical.followup import scan_due
    due = scan_due(store, as_of=args.as_of or None)
    print(f"到期随访：{len(due)} 项")
    for d in due:
        print(f"  #{d['id']} 患者{d['patient_id']} 到期日{d['due_date']} {d['reason'] or ''}")
elif args.cmd == "followup-adherence":
    from .clinical.followup import record_adherence
    record_adherence(store, args.plan_id, args.attended, args.note)
    print(f"随访 #{args.plan_id} 依从性记录完成")
```

- [ ] **步骤 7：测试验证**

运行：`python3 -m pytest tests/test_followup.py tests/test_api.py -v`
预期：ALL PASS

- [ ] **步骤 8：Commit**

```bash
git add khub/api.py khub/cli.py
git commit -m "feat(followup): API + CLI 端点——followup-add/scan/adherence"
```

---

### 任务组 5：病历结构化抽取（`extract.py`）

**文件：**
- 创建：`khub/clinical/extract.py`
- 创建：`tests/test_extract.py`
- 修改：`khub/api.py`
- 修改：`khub/cli.py`

- [ ] **步骤 1：创建 `extract.py`**

```python
"""0.2.7 病历/问诊结构化抽取（LLM + 离线词典兜底）。"""
from __future__ import annotations
from typing import Optional
import re
import json

from ..db import Store
from ..crypto import get_cipher, dec
from ..llm import get_provider

# 从 importer 复用的中文字段同义词映射
_FIELD_SYNONYMS = {
    "辨证": "differentiation_norm", "证型": "differentiation_norm", "中医辨证": "differentiation_norm",
    "方剂": "formula", "处方": "formula", "中药方": "formula",
    "治法": "method", "治疗": "method", "方案": "method",
}

def extract_structured(store: Store, text: str) -> dict:
    """抽取结构化字段，LLM 路径失败时退词典/正则。"""
    provider = get_provider()
    try:
        result = _llm_extract(provider, text)
        if result and _validate_struct(result):
            return result
    except Exception:
        pass
    return _regex_extract(text)

def _llm_extract(provider, text: str) -> dict:
    prompt = (
        f"从以下中医病历/问诊文本中抽取结构化字段，以 JSON 返回（键："
        f"differentiation_norm, syndrome, formula, method）：\n\n{text[:2000]}"
    )
    raw = provider.complete(prompt) or ""
    if not raw:
        return {}
    # 尝试提取 JSON
    m = re.search(r"\{.*\}", raw, re.DOTALL)
    if m:
        return json.loads(m.group())
    return {}

def _regex_extract(text: str) -> dict:
    """基于词典/正则的离线兜底抽取。"""
    result = {}
    # 证型/辨证：查关键词
    if re.search(r"(风寒|风热|寒湿|湿热|痰湿|气滞|血瘀|气虚|血虚|阴虚|阳虚|脾虚|肾虚|肝郁)", text):
        result["differentiation_norm"] = _detect_syndrome(text)
    # 方剂：查 "方：" "剂：" "处[^]*方" 等模式
    fm = re.search(r"(?:方|剂|处方)[：:]\s*(.{2,20})", text)
    if fm:
        result["formula"] = fm.group(1).strip()
    # 治法
    mm = re.search(r"(?:治法|疗法|方案)[：:]\s*(.{2,30})", text)
    if mm:
        result["method"] = mm.group(1).strip()
    return result

def _detect_syndrome(text: str) -> str:
    """从文本检测主要证型（最简规则）。"""
    types = {
        "风寒": "风寒表证", "风热": "风热表证", "寒湿": "寒湿证",
        "湿热": "湿热证", "痰湿": "痰湿证", "气滞": "气滞证",
        "血瘀": "血瘀证", "气虚": "气虚证", "血虚": "血虚证",
        "阴虚": "阴虚证", "阳虚": "阳虚证", "脾虚": "脾虚证",
        "肾虚": "肾虚证", "肝郁": "肝郁证",
    }
    for kw, name in types.items():
        if kw in text:
            return name
    return ""

def _validate_struct(d: dict) -> bool:
    return bool(d.get("differentiation_norm") or d.get("formula") or d.get("method"))

def apply_struct(store: Store, source: str, source_id: int, structured: dict):
    store.conn.execute(
        "INSERT INTO record_struct (source, source_id, differentiation_norm, syndrome, formula, method) "
        "VALUES (?, ?, ?, ?, ?, ?)",
        (source, source_id,
         structured.get("differentiation_norm", ""),
         structured.get("syndrome", ""),
         structured.get("formula", ""),
         structured.get("method", ""))
    )
```

- [ ] **步骤 2：编写测试**

```python
# tests/test_extract.py
import pytest
from khub.db import Store
from khub.clinical.extract import extract_structured, apply_struct

def test_extract_offline_keyword():
    store = Store(":memory:")
    text = "患者风寒表证，处方：桂枝汤，治法：解表散寒"
    result = extract_structured(store, text)
    assert "differentiation_norm" in result
    assert "风寒表证" in result["differentiation_norm"]
    assert result["formula"] == "桂枝汤" or "桂枝汤" in result.get("formula", "")

def test_extract_offline_no_match():
    store = Store(":memory:")
    result = extract_structured(store, "正常体检")
    assert result == {} or not any(v for v in result.values())

def test_apply_struct():
    store = Store(":memory:")
    apply_struct(store, "record", 1, {"differentiation_norm": "风寒表证", "formula": "桂枝汤"})
    row = store.conn.execute("SELECT * FROM record_struct WHERE source_id=1").fetchone()
    assert row is not None
    assert row["source"] == "record"
    assert row["differentiation_norm"] == "风寒表证"

def test_apply_struct_empty():
    store = Store(":memory:")
    apply_struct(store, "consult", 1, {})
    row = store.conn.execute("SELECT * FROM record_struct WHERE source_id=1").fetchone()
    # 无数据时字段均为 ''
    assert row is not None
```

- [ ] **步骤 3：运行测试验证通过**

运行：`python3 -m pytest tests/test_extract.py -v`
预期：PASS

- [ ] **步骤 4：Commit**

```bash
git add khub/clinical/extract.py tests/test_extract.py
git commit -m "feat(extract): 病历/问诊结构化抽取（LLM + 离线词典兜底）"
```

- [ ] **步骤 5：在 `api.py` 新增 `POST /clinical/extract`**

```python
# 0.2.7 clinical 增强 — 结构化抽取
if method == "POST" and path == "/clinical/extract":
    from .clinical.extract import extract_structured, apply_struct
    source = body.get("source", "")
    source_id = body.get("source_id", 0)
    text = body.get("text", "")
    if not source or not source_id:
        return 400, {"error": "source and source_id required"}
    # 如未提供 text，从 records/consultations 读取
    if not text:
        if source == "record":
            row = store.conn.execute(
                "SELECT diagnosis, prescription FROM records WHERE id=?", (source_id,)
            ).fetchone()
            if row: text = f"{row['diagnosis']} {row['prescription']}"
        elif source == "consult":
            row = store.conn.execute(
                "SELECT chief_complaint, differentiation FROM consultations WHERE id=?", (source_id,)
            ).fetchone()
            if row: text = f"{row['chief_complaint']} {row['differentiation']}"
    if not text:
        return 404, {"error": "source not found or text empty"}
    struct = extract_structured(store, text)
    apply_struct(store, source, source_id, struct)
    return 200, {"structured": struct}
```

- [ ] **步骤 6：在 `cli.py` 新增 `record-extract` / `consult-extract`**

```python
# build_parser()
pr = sub.add_parser("record-extract", help="抽取病历结构化字段")
pr.add_argument("record_id", type=int)

pc = sub.add_parser("consult-extract", help="抽取问诊结构化字段")
pc.add_argument("consult_id", type=int)

# main()
elif args.cmd == "record-extract":
    from .clinical.extract import extract_structured, apply_struct
    row = store.conn.execute(
        "SELECT id, diagnosis, prescription FROM records WHERE id=?", (args.record_id,)
    ).fetchone()
    if not row: print(f"病历 #{args.record_id} 不存在"); return
    text = f"{row['diagnosis'] or ''} {row['prescription'] or ''}"
    struct = extract_structured(store, text)
    apply_struct(store, "record", row["id"], struct)
    print(json.dumps(struct, ensure_ascii=False, indent=2))
elif args.cmd == "consult-extract":
    from .clinical.extract import extract_structured, apply_struct
    row = store.conn.execute(
        "SELECT id, chief_complaint, differentiation FROM consultations WHERE id=?", (args.consult_id,)
    ).fetchone()
    if not row: print(f"问诊 #{args.consult_id} 不存在"); return
    text = f"{row['chief_complaint'] or ''} {row['differentiation'] or ''}"
    struct = extract_structured(store, text)
    apply_struct(store, "consult", row["id"], struct)
    print(json.dumps(struct, ensure_ascii=False, indent=2))
```

- [ ] **步骤 7：测试验证**

运行：`python3 -m pytest tests/test_extract.py tests/test_api.py -v`
预期：ALL PASS

- [ ] **步骤 8：Commit**

```bash
git add khub/api.py khub/cli.py
git commit -m "feat(extract): API POST /clinical/extract + CLI record/consult-extract"
```

---

### 任务组 6：集成收尾与发布循环

**文件：**
- 修改：`khub/__init__.py`
- 修改：`pyproject.toml`
- 修改：`CHANGELOG.md`
- 新增 tag + gh release

- [ ] **步骤 1：Bump 版本号**

```bash
# khub/__init__.py
__version__ = "0.2.7"

# pyproject.toml
version = "0.2.7"

# tests/test_api.py — 同步健康检查断言
assert obj["version"] == "0.2.7"
```

- [ ] **步骤 2：运行全量测试**

运行：`python3 -m pytest -q`
预期：ALL PASS（用例数增长，>= 前值 283）

- [ ] **步骤 3：提交版本 bump + CHANGELOG**

```bash
git add khub/__init__.py pyproject.toml tests/test_api.py CHANGELOG.md
git commit -m "chore(release): v0.2.7——临床孪生增强（四线并行）"
git tag v0.2.7
```

- [ ] **步骤 4：推送 + 发布**

```bash
git push origin master
git push origin v0.2.7
gh release create v0.2.7 --title "khub 0.2.7" --notes-file /tmp/release_v0.2.7.md
```
